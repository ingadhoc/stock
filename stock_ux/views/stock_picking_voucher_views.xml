<?xml version="1.0"?>
<odoo>
    <!-- Action to open the label wizard from picking -->
    <record id="action_product_label_layout_picking" model="ir.actions.act_window">
        <field name="name">Print Transfer Labels</field>
        <field name="res_model">product.label.layout</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="product_label_layout_form_view_picking"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
    </record>

    <!-- Add Print Labels button to stock.picking form view -->
    <record id="view_picking_form_print_labels" model="ir.ui.view">
        <field name="name">stock.picking.form.print.labels</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <!-- Add Print Labels button after Validate button -->
            <button name="button_validate" position="after">
                <button name="%(action_product_label_layout_picking)d"
                        string="Print Labels"
                        type="action"
                        class="btn-secondary"
                        invisible="state not in ('assigned', 'done')"
                        icon="fa-print"/>
            </button>
        </field>
    </record>

    <!-- Add vouchers field to tree view (for display purposes) -->
    <record id="view_picking_tree_vouchers" model="ir.ui.view">
        <field name="name">stock.picking.tree.vouchers</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <field name="origin" position="after">
                <field name="vouchers" optional="hide" string="Labels Printed"/>
            </field>
        </field>
    </record>
</odoo>
